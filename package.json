{
  "name": "lora20-indexer",
  "version": "0.1.0",
  "private": true,
  "description": "LoRaWAN token inscription indexer with signed uplink verification and off-chain state.",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node test/indexer-service.test.js"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
